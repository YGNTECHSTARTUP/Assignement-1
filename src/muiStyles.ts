export const formStyles = {
    display: 'flex',
    flexDirection: 'column',
    gap: 2, // gap between children (theme spacing value, e.g., 2 means 16px if default spacing is 8px)
    padding: 3, // padding inside the form (theme spacing value, e.g., 3 means 24px if default spacing is 8px)
    margin: 2, // margin around the form (theme spacing value, e.g., 2 means 16px if default spacing is 8px)
    alignItems: 'center', // center align items vertically
    justifyContent: 'space-between', 
    flexbais:'1',
    border:'1px solid black'

};
 export const containerStyles = {
    display: 'flex',
    flexDirection: 'column', // or 'row' if you want to align items horizontally
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh', 
    width:'100vw'
    

};